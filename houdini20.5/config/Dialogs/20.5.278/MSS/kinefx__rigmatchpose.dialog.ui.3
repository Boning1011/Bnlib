// Magic: 55cca2c4
// Automatically generated PSI dialog (PSI version 20.5.278)
// Thu Jul 11 16:54:30 2024


f_tmp = FLOAT_FIELD;	// dialog.top.ui

enable_t.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    enable_t.label = PARM_LABEL_SIMPLE("Translate", "enable_t") ;
    { WIDTH(0.09) }

    enable_t.menu;

    {  }

    {  }
}

enable_r.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    enable_r.label = PARM_LABEL_SIMPLE("Rotate", "enable_r") ;
    { WIDTH(0.09) }

    enable_r.menu;

    {  }

    {  }
}

enable_s.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    enable_s.label = PARM_LABEL_SIMPLE("Scale", "enable_s") ;
    { WIDTH(0.09) }

    enable_s.menu;

    {  }

    {  }
}

mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    mode.label = PARM_LABEL_SIMPLE("Mode", "mode") ;
    { WIDTH(0.09) }

    mode.menu;

    {  }

    {  }
}

tweak_mode.gad = ROW
{
    
    {  }

    {  }
    { WIDTH(0.08) }

    PARM_BUTTON("tweak_mode", "Tweak Rotate Links") toggle VALUE(tweak_mode.val);

    {  }

    {  }
}

# warning cpp warning: "GenV3/subdialog.top.ui":11 Macro "PARMSPACING" redefined.  Using later definition
# warning cpp warning: "GenV3/subdialog.top.ui":12 Macro "LAYOUTDIR" redefined.  Using later definition
# warning cpp warning: "GenV3/subdialog.top.ui":13 Macro "DO_JUSTIFY" redefined.  Using later definition
dialog_norm.gad = 
{
    LAYOUT(horizontal) SPACING(0.05) HSTRETCH JUSTIFY(left, center) UNSCALABLE
    enable_t.gad; 
    enable_r.gad; 
    enable_s.gad; 
    mode.gad; 
    tweak_mode.gad; 
} 
// dialog.bot.ui

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) VALUE(open.val_) HSTRETCH

    dialog_norm.gad;
}

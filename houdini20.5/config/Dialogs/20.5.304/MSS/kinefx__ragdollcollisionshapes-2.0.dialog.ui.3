// Magic: 8f32eefd
// Automatically generated PSI dialog (PSI version 20.5.304)
// Mon Jul 29 10:32:27 2024


f_tmp = FLOAT_FIELD;	// dialog.top.ui

enable_handle.gad = ROW
{
    
    {  }

    {  }
    { WIDTH(0.08) }

    PARM_BUTTON("enable_handle", "Enable Handle") toggle VALUE(enable_handle.val);

    {  }

    {  }
}

separator6.gad = 

{
    LAYOUT(horizontal) VSTRETCH

    { WIDTH(.1) }
    SEPARATOR vertical; 
    { WIDTH(0.1) }
}
shape.row.join =		
{

  LAYOUT(horizontal) JUSTIFY(left, center) UNSCALABLE

shape.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    shape.label = PARM_LABEL_SIMPLE("Shape", "shape") ;
    { WIDTH(0.09) }

    shape.menu;

    {  }

    {  }
}

algorithm.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    algorithm.label = PARM_LABEL_SIMPLE("Algorithm", "algorithm") ;
    { WIDTH(0.09) }

    algorithm.menu;

    {  }

    {  }
}
}	// Template join.bot


compute_shape_transform.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    compute_shape_transform.label = PARM_LABEL_SIMPLE("Compute Shape Transform", "compute_shape_transform") ;
    { WIDTH(0.09) }

    compute_shape_transform.menu;

    {  }

    {  }
}

# warning cpp warning: "GenV3/subdialog.top.ui":11 Macro "PARMSPACING" redefined.  Using later definition
# warning cpp warning: "GenV3/subdialog.top.ui":12 Macro "LAYOUTDIR" redefined.  Using later definition
# warning cpp warning: "GenV3/subdialog.top.ui":13 Macro "DO_JUSTIFY" redefined.  Using later definition
dialog_norm.gad = 
{
    LAYOUT(horizontal) SPACING(0.05) HSTRETCH JUSTIFY(left, center) UNSCALABLE
    enable_handle.gad; 
    separator6.gad; 
    shape.row.join; 
    compute_shape_transform.gad; 
} 
// dialog.bot.ui

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) VALUE(open.val_) HSTRETCH

    dialog_norm.gad;
}
